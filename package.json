{
  "name": "@isconl/media",
  "version": "0.1.0",
  "private": false,
  "description": "isconl-agent engine: local+OneDrive media playback -- browse, signed-ticket streaming, LRU listing cache",
  "main": "src/server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "@bitwarden/sdk-napi": "^1.0.0"
  },
  "license": "MIT"
}
